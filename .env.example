# Supabase analytics - PUBLIC client config.
# The publishable key is safe to expose in the browser because the events table
# is RLS insert-only for public writes. Never put a service-role key here.
# These defaults are also baked into src/logic/analytics.js; override only when
# rotating keys or pointing to another Supabase project.
VITE_SUPABASE_URL=https://bvajkdxteubiwslgznlt.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_5oJ9TTm8Y2CYXUrJ6C6rpw_0KPIq60N
